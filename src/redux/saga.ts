// import { all, fork, call, put, takeLatest } from "redux-saga/effect";
 
function* rootSaga() {
 // yield all([takeLatest(enumTypes.FETCH_POST_REQUEST, fetchGetAllUserData)]);
}

export { rootSaga };
